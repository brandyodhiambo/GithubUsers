package com.adhanjadevelopers.githubusers.adapters

import androidx.recyclerview.widget.RecyclerView

class UsersAdapter {
    inner class MyHolder(private val binding):RecyclerView.ViewHolder(binding.root)
}